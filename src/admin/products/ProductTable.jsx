import { Link } from "react-router-dom";

const ProductTable = ({ rows, onEdit, onDelete }) => <div className="table-shell"><div className="table-scroll"><table className="min-w-full border-collapse text-sm"><thead><tr><th className="th-cell">Image</th><th className="th-cell">Title</th><th className="th-cell">Brand</th><th className="th-cell">Category</th><th className="th-cell">Price</th><th className="th-cell">Inventory</th><th className="th-cell">Status</th><th className="th-cell">Actions</th></tr></thead><tbody>{rows.map((row) => <tr key={row.product_id} className="hover:bg-slate-50/80"><td className="td-cell">{row.primary_image ? <img src={row.primary_image} alt={row.title} className="h-12 w-12 rounded-xl bg-slate-100 object-cover" /> : <div className="grid h-12 w-12 place-items-center rounded-xl bg-slate-100 text-xs text-slate-500">N/A</div>}</td><td className="td-cell">{row.title}</td><td className="td-cell">{row.brand || "—"}</td><td className="td-cell">{row.category_name || row.category_id || "—"}</td><td className="td-cell">{row.min_price || "—"}</td><td className="td-cell">{row.inventory_total ?? "—"}</td><td className="td-cell">{row.status || "draft"}</td><td className="td-cell"><div className="flex flex-wrap gap-2"><Link to={`/admin/products/${row.product_id}/view`} className="btn-outline">View</Link><button type="button" onClick={() => onEdit(row.product_id)} className="btn-secondary">Edit</button><button type="button" onClick={() => onDelete(row.product_id)} className="btn-outline text-rose-600 hover:border-rose-200 hover:text-rose-700">Delete</button></div></td></tr>)}{!rows.length && <tr><td className="td-cell text-slate-500" colSpan={8}>No products found.</td></tr>}</tbody></table></div></div>;

export default ProductTable;